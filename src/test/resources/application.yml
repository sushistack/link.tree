spring:
  application.name: link.tree
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: ''
    driver-class-name: org.h2.Driver
  h2.console.enabled: true
  jpa:
    hibernate.ddl-auto: create-drop
  batch:
    jdbc.initialize-schema: always
    job.enabled: false

feign:
  client:
    config:
      postClient:
        connectTimeout: 5000
        readTimeout: 5000
        retryer:
          period: 100
          maxPeriod: 1000
          maxAttempts: 3

jasypt:
  encryptor:
    password: ${jasyptPassword}
    algorithm: PBEWithMD5AndDES

decorator:
  datasource:
    p6spy:
      logging: slf4j
      log-format: '%(currentTime)|%(executionTime)ms|connection %(connectionId)|[%(category)]:%(sql)'
      log-filter:
        pattern: '^(?!.*(BATCH_JOB_|BATCH_STEP_)).*$'

# for test
test:
  bitbucket:
    username: ENC(R/zYZt2Fi7FwWkLRC7KPEU3qC9FV/1qMgDHADZTl8Bc=)
    app-password: ENC(TCgsIZrdInsso9ZNZYdPnkUytqujMQdqRZYMw+NDk9uvMUt+yo8wvN1CYjUeiy1ZXvjdlgdapis=)
